import App from './components/app'

const app = new App(document.getElementById('container'), document.querySelector('.modal-container'), document.querySelector('.intro-modal-container'))

app.start()
